MDScreenManager:
    id: shell_sm
    on_kv_post: self.current = "dashboard"

    # Página inicial = Dashboard NOVO
    DashboardScreen:
        name: "dashboard"

    # Esportes favoritos
    ChooseSportsScreen:
        name: "favorites"